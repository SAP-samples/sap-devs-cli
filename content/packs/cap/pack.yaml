id: cap
name: SAP Cloud Application Programming Model
description: Node.js and Java framework for building cloud-native business applications on BTP
tags: [cloud, btp, nodejs, java, odata, cds]
profiles: [cap-developer, btp-developer]
weight: 100
locales:
  de:
    name: SAP Cloud Application Programming Model
    description: Node.js- und Java-Framework für Cloud-native Business-Anwendungen auf BTP
versions:
  "@sap/cds": "9.8.0"
  "@sap/cds-dk": "9.8.0"
changelog:
  - "CAP 9.8: native SQLite support via `cds.requires.db.driver: node` (Node 22.5+)"
  - "CAP 9.8: new `cds repl --ql` query mode for interactive CQL"
  - "CAP 10 (June 2026): Node.js 22 minimum; several `compat_*` flags removed and defaults flipped — see Preparing for CAP 10 in context"
  - "CAP 10 Java: Application Services serve only `odata-v2/v4/x4` by default — annotate services or set `cds.protocols.defaults`"

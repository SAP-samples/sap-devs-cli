id: continue
name: Continue.dev
type: file-inject
targets:
  - scope: global
    path: "~/.continue/config.md"
    mode: replace-section
    section: "SAP Developer Context"
  - scope: project
    path: ".continue/config.md"
    mode: replace-section
    section: "SAP Developer Context"
detect:
  - path: "~/.continue"
